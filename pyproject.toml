[project]
name = "ipados_batteryhealth"
dynamic = ["version"]
requires-python = ">= 3.12"

[dependency-groups]
dev = [
    "mypy>=1.17.1",
    "pytest>=8.4.1",
    "pytest-coverage>=0.0",
    "ruff>=0.12.7",
    "ty>=0.0.1a16",
]
